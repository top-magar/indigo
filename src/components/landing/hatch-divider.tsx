/**
 * Engineering-style hatch band used between major sections.
 *
 * Full-width repeating diagonal hairlines, thin top/bottom borders, low
 * contrast — the drafting-sheet divider that gives the page its rhythm.
 */
export function HatchDivider() {
  return <div className="lv2-hatch" role="presentation" aria-hidden />;
}
