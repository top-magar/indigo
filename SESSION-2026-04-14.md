# Session Checkpoint — 2026-04-14

## Branch: `editor/core-plugin-api`
## Commits today: 49 (net ~35 after reverts)
## TS Errors: 0

---

## What was accomplished

### Phase 1: Editor V3 UI Polish (4 commits)
- Style panel section headers — chevron animation fix
- Box model editor, navigator tree, dimensions row polish
- Style panel property rows — segmented buttons, hover states, focus rings
- All editor routes now disabled (redirected to dashboard)

### Phase 2: Product Discovery + Architecture (2 docs)
- `docs/PRODUCT-DISCOVERY.md` — problem validation, 3 personas, scope, architecture
- `docs/ARCHITECTURE-REVIEW.md` — full audit (5.8→8.2/10), 8 critical issues, execution plan

### Phase 3: Security Hardening (1 commit)
- Editor auth bypass fixed
- CSP header with payment domain allowlists
- RBAC middleware (owner/admin/editor roles)
- Connection pooler for serverless
- Nepali locale + full dictionary (90 translations)

### Phase 4: Data Integrity (1 commit)
- `withPlatformAdmin` requires AdminContext + audit logging
- Tenant-scoped composite indexes on orders
- Unique constraint on customers(tenant_id, email)
- `updatedAt` auto-trigger SQL migration

### Phase 5: Payment Providers (3 commits)
- eSewa provider — HMAC-SHA256 signature, redirect flow, status verification
- Khalti provider — server-side initiation, redirect, lookup verification
- Payment settings UI — eSewa/Khalti merchant code + secret key fields
- Providers read credentials from tenant settings (per-merchant keys)
- Checkout form wired for redirect flow
- Payment callback route for eSewa/Khalti

### Phase 6: Messaging + COD (2 commits)
- WhatsApp provider (Meta Business API)
- SMS provider (Sparrow SMS)
- Notification delivery extended with whatsapp/sms channels
- COD reconciliation schema (cod_collections + delivery_attempts)
- Inngest job: order → WhatsApp to merchant + SMS to customer

### Phase 7: Performance + Infrastructure (2 commits)
- Upstash Redis rate limiter (replaces in-memory no-op)
- Upstash Redis server cache
- Storefront ISR caching (5min TTL, parallel queries)
- Dashboard decomposed (438→114 lines)

### Phase 8: Drizzle Migration (5 commits)
- Dashboard queries → Drizzle `withTenant()` (11 Supabase calls removed)
- Store layout → Drizzle (3 calls)
- Store page + generateMetadata → Drizzle (4 calls)
- Products data layer → Drizzle with LEFT JOIN (5 functions)
- Categories data layer → Drizzle with subquery counts (4 functions)
- Layout service → Drizzle raw SQL (2 functions)

### Phase 9: Store Customization (6 commits, 5 reverted)
- Section registry — 8 types × 2-4 variants = ~25 design options
- Section builder UI — pick sections, choose variants, edit content, reorder
- Section renderer — renders hero, products, categories, banners, testimonials
- Store appearance settings — theme, colors, fonts, hero, branding, SEO
- Storefront wired to read settings (CSS variables, Google Fonts)
- **Reverted**: side-by-side preview layout (needs better approach)

---

## Current State

### What works
- Dashboard at `/dashboard` — fully functional, Drizzle-backed
- Store at `/store/[slug]` — renders from section config OR default homepage
- Section builder at `/dashboard/settings/storefront` — Sections + Branding tabs
- Payment settings with eSewa/Khalti credential management
- Checkout with eSewa/Khalti redirect flow
- WhatsApp/SMS notification infrastructure (needs API keys to deliver)
- COD reconciliation schema (needs dashboard UI)
- Nepali locale configured (needs translation QA)

### What's disabled
- All editors (`/editor-v3`, `/storefront`) — redirected to dashboard
- Old `StoreShell` homepage skip logic — always renders header/footer now

### What needs work next
1. **Live preview for section builder** — reverted, needs a cleaner approach
2. **Store themes** — 5 theme variants defined but no actual different layouts
3. **Mobile dashboard** — responsive sidebar + touch targets
4. **COD reconciliation dashboard UI** — schema exists, no page
5. **Remaining Supabase migration** — ~350 calls in dashboard server actions
6. **Image upload** — section builder uses URL paste, should use Media Library

### Env vars needed for production
```
ESEWA_MERCHANT_CODE, ESEWA_SECRET
KHALTI_SECRET_KEY
WHATSAPP_PHONE_NUMBER_ID, WHATSAPP_ACCESS_TOKEN
SPARROW_SMS_TOKEN, SPARROW_SMS_FROM
UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN
SENTRY_DSN, NEXT_PUBLIC_SENTRY_DSN
POOLER_DATABASE_URL
```

### Architecture score: ~8.2/10 (was 5.8)
